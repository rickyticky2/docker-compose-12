FROM tomcat
WORKDIR /usr/local/tomcat/
RUN mkdir /usr/local/tomcat/webapps/ROOT
EXPOSE 8080
RUN cp /build/target/*.war webapps/ && cp /build/Config.properties webapps/ROOT/ 
CMD ["catalina.sh", "run"]
